# Local Docker credentials (not for production).
# Copy before first run: cp .env.docker.example .env.docker
# Set POSTGRES_PASSWORD and DB_PASSWORD to the same value.

POSTGRES_DB=ecomshop
POSTGRES_USER=ecomshop
POSTGRES_PASSWORD=

DB_DATABASE=ecomshop
DB_USERNAME=ecomshop
DB_PASSWORD=
